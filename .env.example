# Nimiq Node IP address
NODE_IP=127.0.0.1

# Nimiq Node RPC port
NODE_PORT=8648

# Network to use (main/test)
NETWORK=test

# Length of cashlink tokens in characters
TOKEN_LENGTH=6

# Salt for cashlink generation (base64 encoded)
SALT=your-base64-encoded-salt-here